import {Routes} from '@angular/router';
import {AddShopComponent} from './addShop.component';

export const addShopRouter: Routes = [
    {
        path: 'customer/query',
        component: AddShopComponent
    }
];
