import {Routes} from '@angular/router';
import {ShopComponent} from './addShop.component';

export const addShopRouter: Routes = [
    {
        path: 'customer/query',
        component: ShopComponent
    }
];
