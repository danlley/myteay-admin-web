import {Routes} from '@angular/router';
import {ProviderComponent} from './provider.component';

export const providerRouter: Routes = [
    {
        path: 'system/provider',
        component: ProviderComponent
    }
];
