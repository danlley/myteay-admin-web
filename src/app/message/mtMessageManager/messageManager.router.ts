import {Routes} from '@angular/router';
import {MessageComponent} from '../message.component';

export const messageManagerRouter: Routes = [
    {
        path: '',
        component: MessageComponent
    }
];
