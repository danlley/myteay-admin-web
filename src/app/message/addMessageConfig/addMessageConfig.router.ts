import {Routes} from '@angular/router';
import {AddMessageConfigComponent} from './addMessageConfig.component';

export const addMessageConfigRouter: Routes = [
    {
        path: '',
        component: AddMessageConfigComponent
    }
];
