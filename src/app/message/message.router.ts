import {Routes} from '@angular/router';
import {MessageComponent} from './message.component';

export const messageRouter: Routes = [
    {
        path: '',
        component: MessageComponent
    }
];
